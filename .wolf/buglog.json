{
  "version": 1,
  "bugs": [
    {
      "id": "bug-001",
      "timestamp": "2026-04-29T19:30:00Z",
      "error_message": "The given repository URL does not look like a valid KiCad package repository. Please double check the URL.",
      "file": "repository.json (hosted at raw.githubusercontent.com/Wadam100/via-stitching/main/repository.json)",
      "root_cause": "GitHub repository Wadam100/via-stitching was private. KiCad PCM fetches repository.json anonymously and cannot send credentials, so raw.githubusercontent.com returned 404 and KiCad reported the URL as invalid. Local git ls-remote worked because it used the user's stored credentials, hiding the visibility issue.",
      "fix": "Make the GitHub repository public (Settings -> Danger Zone -> Change visibility, or `gh repo edit Wadam100/via-stitching --visibility public --accept-visibility-change-consequences`). Verify with anonymous curl/WebFetch of the raw repository.json URL before retrying in KiCad PCM.",
      "tags": [
        "kicad",
        "pcm",
        "github",
        "visibility",
        "private-repo",
        "raw.githubusercontent.com"
      ],
      "related_bugs": [],
      "occurrences": 1,
      "last_seen": "2026-04-29T19:30:00Z"
    },
    {
      "id": "bug-002",
      "timestamp": "2026-04-29T17:58:00Z",
      "error_message": "KiCad 10 PCM 'Fetching repository' never finishes — dialog hangs with no error message when the user adds the custom repository URL.",
      "file": "repository.json, packages.json, metadata.json, tools/build_pcm_release.py",
      "root_cause": "Repository was published using PCM schema v1 ($schema=https://go.kicad.org/pcm/schemas/v1, no schema_version field). KiCad 10 introduced PCM schema v2 and its v2-aware client hangs on the fetch step instead of erroring when it can't validate the repository as v2. Additionally the package versions had `kicad_version_max: \"10.0\"` which the PCM treats as exclusive, hiding the package from KiCad 10 even after the fetch.",
      "fix": "Bumped both files' `$schema` to https://go.kicad.org/pcm/schemas/v2 and added `\"schema_version\": 2` at the top of repository.json. Removed `kicad_version_max` from metadata.json (plugin works on KiCad 9+ with the stable pcbnew SWIG API), added explicit `\"runtime\": \"swig\"` per the v2 schema. Updated `tools/build_pcm_release.py` to emit v2 artifacts and re-ran it so packages.json/repository.json/zip carry consistent SHA-256s.",
      "tags": [
        "kicad",
        "kicad-10",
        "pcm",
        "schema-v2",
        "fetching-repository",
        "hang",
        "kicad_version_max",
        "runtime",
        "swig"
      ],
      "related_bugs": [
        "bug-001"
      ],
      "occurrences": 1,
      "last_seen": "2026-04-29T17:58:00Z"
    },
    {
      "id": "bug-003",
      "timestamp": "2026-04-29T18:10:00Z",
      "error_message": "KiCad PCM Install button is not working / grayed out; tool does not appear in pcbnew Tools menu after installation.",
      "file": "metadata.json, packages.json, via_stitching/plugin.py",
      "root_cause": "Two combined issues: (1) Package status was 'development' — KiCad PCM hides or grays out the Install button for development packages unless the user explicitly enables 'Show development versions' in PCM preferences, so the user could never click Install. (2) plugin.py defaults() only set self.icon_file_name when icon.png existed on disk; since icon.png is not in the zip, the attribute was never set. On KiCad versions where register() tries to read icon_file_name this raised AttributeError and the plugin silently failed to register, so it never appeared in Tools.",
      "fix": "1. Changed status from 'development' to 'stable' in metadata.json so the Install button is active by default. 2. Added explicit self.icon_file_name = '' before the conditional in plugin.py defaults() so the attribute is always present. 3. Re-ran build_pcm_release.py to regenerate zip, packages.json, and repository.json with updated metadata and plugin code.",
      "tags": [
        "kicad",
        "pcm",
        "install-button",
        "status-development",
        "icon_file_name",
        "ActionPlugin",
        "register",
        "tools-menu"
      ],
      "related_bugs": [
        "bug-002"
      ],
      "occurrences": 1,
      "last_seen": "2026-04-29T18:10:00Z"
    },
    {
      "id": "bug-004",
      "timestamp": "2026-04-30T16:34:47.307Z",
      "error_message": "Plugin installed via PCM but does not appear in pcbnew Tools menu",
      "file": "tools/build_pcm_release.py",
      "root_cause": "build_pcm_release.py line 117 packed plugin files as `plugins/via_stitching/__init__.py` (with an extra PLUGIN_PKG_DIR nesting). PCM extracted this to com_github_wadam100_via-stitching/via_stitching/ with nothing at the plugin root. KiCad scans each plugin directory for __init__.py at the root — it never found one so the plugin was never registered.",
      "fix": "Removed PLUGIN_PKG_DIR from the arc path so files pack as plugins/__init__.py (flat). Bumped version to 0.1.1, rebuilt zip + packages.json + repository.json. Also manually copied the 4 py files from the installed via_stitching/ subfolder up to the plugin root so the current install works immediately without a full PCM reinstall.",
      "tags": [
        "kicad",
        "pcm",
        "tools-menu",
        "plugin-not-found",
        "zip-layout",
        "build-script"
      ],
      "related_bugs": ["bug-003"],
      "occurrences": 1,
      "last_seen": "2026-04-30T18:35:00Z"
    }
  ]
}